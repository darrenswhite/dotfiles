#!/usr/bin/env bash

set -e

sudo apt update
sudo apt install
sudo apt upgrade
sudo apt dist-upgrade
sudo apt autoremove
sudo apt autoclean

if [[ -f /run/reboot-required ]]; then
    cat /run/reboot-required
fi
